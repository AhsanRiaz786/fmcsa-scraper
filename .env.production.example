# Database Configuration
DB_NAME=fmcsa_safer
DB_USER=your_user
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432

# Database Connection Pool (optimized for 4 cores)
DB_POOL_MAX=20
DB_POOL_MIN=5

# Proxy Configuration
PROXY_URL=aus.360s5.com:3600
PROXY_USER_BASE=80986603-zone-custom
PROXY_PASS=your_password
PROXY_SESS_TIME=30

# Performance Settings (optimized for 4-core, 8GB RAM server)
CONCURRENCY=200
PROXY_MAX_CONCURRENCY=250
BATCH_SIZE=1000
MAX_RETRIES=3
REQUEST_TIMEOUT=15000

# Test Mode
TEST_MODE=false
TEST_LIMIT=500

# Input File
INPUT_FILE=dot_numbers.csv
